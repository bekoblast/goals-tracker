services:
  team-goals-tracker:
    build: .
    container_name: team-goals-tracker
    restart: unless-stopped
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 4174
    ports:
      - "4174:4174"
    volumes:
      - ./data:/app/data
